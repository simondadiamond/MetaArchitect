# coo — operating memory

> Curated memory for the coo agent. Appended by the agent (dated bullets),
> pruned by Simon. Profile changes are proposed, never self-applied.

## Standing notes
- (seeded 2026-07-11) Chief Operating Officer for The Meta Architect — owns roadmap, brand enforcement, content pipeline coordination, and the anti-recurrence lessons loop.
- (seeded 2026-07-11) Every response must end with a Next Action line; status changes Simon didn't ask for are propose-only, never a direct write to the goals table.

## Session lessons
- (2026-07-13) When shipping a second PR from the same reused worktree branch after a squash-merge: the remote branch still holds the pre-squash commits, so a plain push is rejected non-fast-forward. Delete the merged remote branch first (`git push origin --delete <branch>` or merge with `--delete-branch`), reset local onto `origin/main`, then push fresh. Never resolve this with a force-push.
- (2026-07-13) When subagents commit their own work in the same worktree, the main session must never `git add -A` — it swept a fix-agent's mid-edit files into an unrelated commit (content survived, attribution and atomicity didn't). Stage by explicit path, always.
- (2026-07-13) The two-auditor rubric test + adversarial ICP buyer review earned their cost on the offer-ladder kit (0-divergence calibration proof; 5 KILL-grade findings including 3 independently invented fake statistics). Confirmed approach for any judgment-dense client-facing artifact set: rubric/spec first, parallel draft, independent-scorer test, ICP attack, then Simon gate.
- (2026-07-16) Run `PATH="$HOME/.local/bin:$PATH" node bench/run.mjs --fast` in ~/projects/brain before AND after any change touching find scoring or note descriptions — descriptions are the lexical search surface, and the bench caught two real recall regressions this session (a scorer guard false-positive and a description that dropped "Simon's older daughter"). Story verify criteria that name a production row get it mutated by the verifier; always author disposable fixtures (rule now in root CLAUDE.md story section).
- (2026-07-19) Check the primary MetaArchitect checkout's sync state (`git status -sb`) at session start: found it ahead 2 / behind 2 — two sessions' doc commits stranded unpushed while origin/main had moved, which silently hides merged skills (build-story was invisible locally) and would break prompt schedules whose working_dir is the primary checkout. A plain `git pull --rebase` + push reconciled it cleanly.
- (2026-07-21) When a validation premise collapses mid-task, re-derive whether the downstream action still has positive expected value before proposing it — don't ship on "zero risk" alone. Zero traffic means zero benefit too; unproven data + real churn cost = park it. Caught by Simon on the /setup copy pass (language bank was vocabulary, not prevalence proof; correct move was: bank as reference asset, discovery-call language as ground truth, page rewritten once from proven words).
- (2026-07-30b) Variant of the 2026-07-13 reused-branch scar: rebasing an already-PUSHED session branch onto origin/main mid-session dead-ends the same way — the push needs force, which hooks correctly deny. Standard pattern now: never rebase a pushed branch; commit new work on a fresh branch cut from origin/main (`git checkout -b <topic> origin/main`) and PR that.
- (2026-07-30) Simon's directives get the same filter as everything else — he called out that I sometimes execute his ideas without stress-testing them first ("you should have a filter on it"). The critique contract (CLAUDE.md behavior 7) is not only for explicit stress-test asks: before executing any non-trivial directive from Simon, briefly name the strongest counter-argument or trade-off, then proceed (or push back). Agreement is allowed; unexamined agreement is not. Don't swing to blocking on trivial asks — the filter is one or two sentences, not a gate review.
- 2026-07-31: Before debugging a just-merged CC feature, check `journalctl --user -u deploy-sync | grep deployed` against the merge commit — story-passed precedes live by ~3 min; that gap is a built-in false-alarm window (lessons.md 2026-07-29).
- 2026-08-03: Simon's standing ask, repeated with emphasis: don't just filter his ideas — actively SCOUT high-leverage plays (automation, cross-posting, systems that multiply his time) and bring them to him, building solo where the night-build lane allows. The filter has two directions: push back on weak premises AND push forward on what he's underselling. A COO who only vets incoming ideas is doing half the job.
- 2026-07-29: Never write a price or offer term from memory. Read simonparis-website `lib/pricing.ts` (or the live page) at point of use before any customer-facing draft. A $150/hr fabrication (canonical: $125 USD) reached Simon's clipboard. Related: pricing ladder is deep-researched twice — currency/price changes are research-gated, not vibes (brain: operator-icp-budget note).
- 2026-08-06: Agents can apply Supabase DDL directly — `~/.supabase/access-token` (sbp_, connects as `postgres`) + `POST https://api.supabase.com/v1/projects/<ref>/database/query` with `{"query": "<sql>"}`; `[]` means success, `<ref>` = subdomain of NEXT_PUBLIC_SUPABASE_URL. No `supabase` CLI and no `psql` on the box, so probing for those wrongly reads as "no DDL access". Never hand a migration to Simon for the SQL editor again (lessons.md 2026-08-06). Corollary: a repo doc asserting a LIMITATION is a claim with an expiry date — probe the capability before offloading a step to Simon.
- 2026-08-06: `deploy-sync` never restarts `term-daemon` (invariant: it holds live Claude PTY sessions, ADE spec 2026-07-10). So any change to `term-daemon/*` is live only after Simon restarts it, which kills active sessions — always report that as a pending step rather than implying the change is live.
- 2026-08-06: Simon's ask — proactively SPOT sellable inclusions, don't wait to be told. The high-ticket offer is a generic Command Center that clients customize plus paid training, so every CC feature is a candidate line item. Whenever a CC feature ships (or an idea lands) that a client would ask about — cost control, model choice, credential handling, anything that removes a scary onboarding step or answers a security question — append it to brain note `client-facing-command-center-feature-wishlist-running-list-a`, framed as the sales talking point rather than the internal mechanic, and name the multi-tenant gate if it's single-tenant-only. This is behavior #9's scouting direction applied to product surface, not just automation.
- 2026-08-18: `brain drop <slug>` does NOT remove the Supabase `brain_entries` row — it deletes the file, rebuilds INDEX.md, and commits, printing only the slug, so it looks fully done. The row (and therefore the note on Command Center's /brain page, Tree tab included) survives until a separate `brain sync` prunes it. Always run `brain sync` after a drop and verify the row is gone; a drop reported as complete while the surface Simon actually looks at still shows the note is a silent inconsistency, not a cosmetic one.
- 2026-08-18: Orient before searching the brain — `curl -s http://100.105.85.5:3737/api/brain/tree` returns the whole map (23 notes: title, slug, domain, tags, description, created_at, and every `##` section heading, sources excluded and counted separately) for ~3.5k tokens since story #163 stripped `body_md` from the payload. Cheaper and richer than a shell walk of `~/projects/brain/notes/`, and it works from any session, including agents with no brain checkout. Do this before `brain find` on anything non-obvious: search only finds what you already suspect exists, browse tells you what's there.
